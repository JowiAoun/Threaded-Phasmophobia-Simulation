#include "defs.h"

void addHunter(char* name, enum EvidenceType equipment, RoomType* room, EvidenceListType* evidenceList){
  HunterType *hunter = (HunterType*)allocMemory(sizeof(HunterType));

  

}

